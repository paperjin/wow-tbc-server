diff --git a/src/game/BattleGround/BattleGround.h b/src/game/BattleGround/BattleGround.h
index 00d55fd0c..a288203df 100644
--- a/src/game/BattleGround/BattleGround.h
+++ b/src/game/BattleGround/BattleGround.h
@@ -82,8 +82,8 @@ enum BattleGroundMarks
 
 enum BattleGroundMarksCount
 {
-    ITEM_WINNER_COUNT               = 3,
-    ITEM_LOSER_COUNT                = 1
+    ITEM_WINNER_COUNT               = 60,
+    ITEM_LOSER_COUNT                = 20
 };
 
 enum BattleGroundSpells
diff --git a/src/game/Entities/Player.cpp b/src/game/Entities/Player.cpp
index 251968923..6ac5a9ba5 100644
--- a/src/game/Entities/Player.cpp
+++ b/src/game/Entities/Player.cpp
@@ -1919,7 +1919,7 @@ void Player::ToggleAFK()
         SetGroupUpdateFlag(GROUP_UPDATE_FLAG_STATUS);
 
     // afk player not allowed in battleground
-    if (isAFK() && InBattleGround() && !InArena() && !IsGameMaster())
+    if (isAFK() && InBattleGround() && !InArena() && !IsGameMaster() && !GetPlayerbotMgr())
         LeaveBattleground();
 
     GetSession()->AfkStateChange(isAFK());
