diff --git a/playerbot/PlayerbotFactory.cpp b/playerbot/PlayerbotFactory.cpp
index 2f03556f..ce894e10 100644
--- a/playerbot/PlayerbotFactory.cpp
+++ b/playerbot/PlayerbotFactory.cpp
@@ -2547,6 +2547,9 @@ bool PlayerbotFactory::CanEquipArmor(ItemPrototype const* proto)
 {
     if (bot->HasSkill(SKILL_SHIELD) && proto->SubClass == ITEM_SUBCLASS_ARMOR_SHIELD)
         return true;
+    // Cloth fallback - any class can wear cloth
+    if (proto->SubClass == ITEM_SUBCLASS_ARMOR_CLOTH)
+        return true;
 
     if (bot->HasSkill(SKILL_PLATE_MAIL))
     {
@@ -3240,7 +3243,7 @@ void PlayerbotFactory::InitEquipment(bool incremental, bool syncWithMaster, bool
             else
             {
                 std::vector<uint32> ids;
-                for (uint32 q = quality; q < ITEM_QUALITY_ARTIFACT; ++q)
+                for (int q = quality; q < (int)ITEM_QUALITY_ARTIFACT; ++q)
                 {
                     // quality selected from command
                     if (setQuality && q != quality)
diff --git a/playerbot/strategy/actions/BattleGroundJoinAction.cpp b/playerbot/strategy/actions/BattleGroundJoinAction.cpp
index 041300b2..8bb6432e 100644
--- a/playerbot/strategy/actions/BattleGroundJoinAction.cpp
+++ b/playerbot/strategy/actions/BattleGroundJoinAction.cpp
@@ -393,7 +393,7 @@ bool BGJoinAction::shouldJoinBg(BattleGroundQueueTypeId queueTypeId, BattleGroun
     if (!hasPlayers && !noLag)
         return false;
 
-    if (ai->HasRealPlayerMaster())
+    if (ai->HasRealPlayerMaster() && !sRandomPlayerbotMgr.GetValue(bot->GetGUIDLow(), "selfbot"))
         return false;
 
 #ifndef MANGOSBOT_ZERO
diff --git a/playerbot/strategy/actions/CheckMountStateAction.cpp b/playerbot/strategy/actions/CheckMountStateAction.cpp
index d21441b4..21192e92 100644
--- a/playerbot/strategy/actions/CheckMountStateAction.cpp
+++ b/playerbot/strategy/actions/CheckMountStateAction.cpp
@@ -273,7 +273,7 @@ bool CheckMountStateAction::isUseful()
         return false;
 
 #ifndef MANGOSBOT_ZERO
-    if (bot->InArena())
+    if (bot->InArena() || (bot->InBattleGround() && sRandomPlayerbotMgr.GetValue(bot->GetGUID().GetCounter(), selfbot)))
         return false;
 #endif
 
diff --git a/playerbot/strategy/actions/ReviveFromCorpseAction.cpp b/playerbot/strategy/actions/ReviveFromCorpseAction.cpp
index 44a2fa8f..09605a52 100644
--- a/playerbot/strategy/actions/ReviveFromCorpseAction.cpp
+++ b/playerbot/strategy/actions/ReviveFromCorpseAction.cpp
@@ -207,7 +207,7 @@ bool FindCorpseAction::Execute(Event& event)
 
             moved = MoveTo(moveToPos.getMapId(), moveToPos.getX(), moveToPos.getY(), moveToPos.getZ(), false, false);
 
-            if (!moved && !ai->HasActivePlayerMaster()) //We could not move to coprse. Try spirithealer instead.
+            if (!moved && (!ai->HasActivePlayerMaster() || sRandomPlayerbotMgr.GetValue(bot->GetGUIDLow(), "selfbot"))) //We could not move to coprse. Try spirithealer instead.
             {
                 moved = ai->DoSpecificAction("spirit healer", Event(), true);
             }
diff --git a/playerbot/strategy/generic/RpgStrategy.cpp b/playerbot/strategy/generic/RpgStrategy.cpp
index a37186b8..1d345b47 100644
--- a/playerbot/strategy/generic/RpgStrategy.cpp
+++ b/playerbot/strategy/generic/RpgStrategy.cpp
@@ -1,4 +1,4 @@
-
+#include <vector>
 #include "playerbot/playerbot.h"
 #include "RpgStrategy.h"
 #include "playerbot/strategy/actions/RpgSubActions.h"
@@ -27,7 +27,23 @@ float RpgActionMultiplier::GetValue(Action* action)
 
 void RpgStrategy::OnStrategyAdded(BotState state)
 {
-    ai->ChangeStrategy("+rpg quest,+rpg vendor,+rpg explore,+rpg maintenance,+rpg player,+rpg bg,+rpg guild,+rpg bank", state);
+    std::vector<std::string> subs = {
+        "rpg quest", "rpg vendor", "rpg explore", "rpg maintenance",
+        "rpg player", "rpg bg", "rpg guild", "rpg bank"
+    };
+
+    std::string toAdd;
+    for (const auto& sub : subs)
+    {
+        if (!ai->HasStrategy(sub, state))
+        {
+            if (!toAdd.empty()) toAdd += ",";
+            toAdd += "+" + sub;
+        }
+    }
+
+    if (!toAdd.empty())
+        ai->ChangeStrategy(toAdd, state);
 }
 void RpgStrategy::OnStrategyRemoved(BotState state) 
 {
